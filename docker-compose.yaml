services:


  frontend:
    build:
      context: .
      dockerfile: ./frontend/Dockerfile
    container_name: astro
    ports:
      - "8081:8081"
    env_file:
      - .env
    networks:
      - i-ai-design-system-network
    

networks:
  i-ai-design-system-network:
    driver: bridge
