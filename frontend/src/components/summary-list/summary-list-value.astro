---
import type { HTMLAttributes } from "astro/types";

interface Props extends HTMLAttributes<"dd"> {}

const { class: className, ...rest } = Astro.props;
---

<dd class={`govuk-summary-list__value${className ? ` ${className}` : ""}`} {...rest}>
    <slot />
</dd>
