---
import type { HTMLAttributes } from "astro/types";

interface Props extends HTMLAttributes<"textarea"> {}

const { class: className, ...rest } = Astro.props;
---

<textarea
    class={`govuk-textarea govuk-js-character-count${className ? ` ${className}` : ""}`}
    {...rest}></textarea>
