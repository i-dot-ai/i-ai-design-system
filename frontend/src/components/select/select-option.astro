---
import type { HTMLAttributes } from "astro/types";

interface Props extends HTMLAttributes<"option"> {}

const { class: className, ...rest } = Astro.props;
---

<option class={className ? ` ${className}` : ""} {...rest}>
    <slot />
</option>
