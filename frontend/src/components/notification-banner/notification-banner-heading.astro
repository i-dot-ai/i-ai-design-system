---
import type { HTMLAttributes } from "astro/types";

interface Props extends HTMLAttributes<"p"> {}

const { class: className, ...rest } = Astro.props;
---

<p class={`govuk-notification-banner__heading${className ? ` ${className}` : ""}`} {...rest}>
    <slot />
</p>
