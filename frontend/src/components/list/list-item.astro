---
import type { HTMLAttributes } from "astro/types";

interface Props extends HTMLAttributes<"li"> {}

const { class: className, ...rest } = Astro.props;
---

<li class={`govuk-list-item${className ? ` ${className}` : ""}`} {...rest}>
    <slot />
</li>
