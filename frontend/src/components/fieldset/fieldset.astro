---
import type { HTMLAttributes } from "astro/types";

interface Props extends HTMLAttributes<"fieldset"> {}

const { class: className, ...rest } = Astro.props;
---

<fieldset class={`govuk-fieldset${className ? ` ${className}` : ""}`} {...rest}>
    <slot />
</fieldset>
