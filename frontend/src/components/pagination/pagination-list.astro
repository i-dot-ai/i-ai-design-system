---
import type { HTMLAttributes } from "astro/types";

interface Props extends HTMLAttributes<"ul"> {}

const { class: className, ...rest } = Astro.props;
---

<ul class={`govuk-pagination__list${className ? ` ${className}` : ""}`} {...rest}>
    <slot />
</ul>
