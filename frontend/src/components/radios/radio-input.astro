---
import type { HTMLAttributes } from "astro/types";

interface Props extends HTMLAttributes<"input"> {}

const { class: className, ...rest } = Astro.props;
---

<input
    class={`govuk-radios__input${className ? ` ${className}` : ""}`}
    type="radio"
    {...rest}
/>
