---
import type { HTMLAttributes } from "astro/types";

interface Props extends HTMLAttributes<"label"> {}

const { class: className, ...rest } = Astro.props;
---

<label
    class={`govuk-label iai-toggle__label${className ? ` ${className}` : ""}`}
    {...rest}
>
    <slot />
</label>
