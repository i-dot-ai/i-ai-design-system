---
import type { HTMLAttributes } from "astro/types";

interface Props extends HTMLAttributes<"a"> {}

const { class: className, ...rest } = Astro.props;
---

<a
    class={`govuk-skip-link${className ? ` ${className}` : ""}`}
    data-module="govuk-skip-link"
    {...rest}
>
    <slot />
</a>
