---
import type { HTMLAttributes } from "astro/types";

interface Props extends HTMLAttributes<"details"> {}

const { class: className, ...rest } = Astro.props;
---

<details class={`govuk-details${className ? ` ${className}` : ""}`} {...rest}>
    <slot />
</details>
