---
import type { HTMLAttributes } from "astro/types";

interface Props extends HTMLAttributes<"a"> {}

const { class: className, ...rest } = Astro.props;
---

<a class={`iai-card__link${className ? ` ${className}` : ""}`} {...rest}>
    <slot />
</a>
