---
name: Select
status: standard
---

## Default

A default select is used to indicate that the select is the main focus of the page.

<Example component="select" astro="./examples/default.astro" html="./examples/default.html" />

## Subtle

A subtle select is used to reduce the prominence of the select component. Used for when the select is not the main focus of the page.

<Example component="select" astro="./examples/subtle.astro" html="./examples/subtle.html" />

## Error

A error select is used to indicate that the select has an error.

<Example component="select" astro="./examples/error.astro" html="./examples/error.html" />

## When to use this component

The select component should only be used as a last resort in public-facing services because research shows that some users find selects very difficult to use.

## When not to use this component

The select component allows users to choose an option from a long list. Before using the select component, try asking users questions which will allow you to present them with fewer options.

Asking questions means you’re less likely to need to use the select component, and can consider using a different solution, such as a [Radios component](/components/radios/).

## Deviation from GOV.UK Design System

Explanation here
