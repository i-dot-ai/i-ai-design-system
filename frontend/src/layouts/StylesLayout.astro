---
import Layout from "@layouts/Layout.astro";
import {
    Breadcrumbs,
    BreadcrumbItem,
    ContentsPanel,
    ContentsPanelList,
    ContentsPanelItem,
    ContentsPanelListHeading,
} from "@i-dot-ai-npm/component-library-astro";

import SideNavTitle from "@components/side-nav/side-nav-title.astro";

const currentPath = Astro.url.pathname;

const structurePages = [
    {
        text: "Public facing landing page",
        href: "/styles/public-facing-landing-page",
    },
    {
        text: "Main application structure",
        href: "/styles/main-application-structure",
    },
];

const brandingPages = [
    {
        text: "Branding guidelines",
        href: "/styles/branding",
    },
];

const visualPages = [
    {
        text: "Colour palette",
        href: "/styles/colour-palette",
    },
    {
        text: "Chart colours",
        href: "/styles/chart-colours",
    },
];
---

<Layout>
    <div class="govuk-main-wrapper govuk-width-container">
        <div class="govuk-grid-row">
            <div class="govuk-grid-column-one-quarter">
                <ContentsPanel>
                    <ContentsPanelListHeading>Branding</ContentsPanelListHeading
                    >
                    <ContentsPanelList>
                        {
                            brandingPages.map((page) => (
                                <ContentsPanelItem
                                    href={page.href}
                                    current={currentPath.includes(page.href)}
                                >
                                    {page.text}
                                </ContentsPanelItem>
                            ))
                        }
                    </ContentsPanelList>
                    <ContentsPanelListHeading
                        >Structure</ContentsPanelListHeading
                    >
                    <ContentsPanelList>
                        {
                            structurePages.map((page) => (
                                <ContentsPanelItem
                                    href={page.href}
                                    current={currentPath.includes(page.href)}
                                >
                                    {page.text}
                                </ContentsPanelItem>
                            ))
                        }
                    </ContentsPanelList>
                    <ContentsPanelListHeading>Colour</ContentsPanelListHeading>
                    <ContentsPanelList>
                        {
                            visualPages.map((page) => (
                                <ContentsPanelItem
                                    href={page.href}
                                    current={currentPath.includes(page.href)}
                                >
                                    {page.text}
                                </ContentsPanelItem>
                            ))
                        }
                    </ContentsPanelList>
                </ContentsPanel>
            </div>
            <div class="govuk-grid-column-three-quarters">
                <Breadcrumbs>
                    <BreadcrumbItem href="/">Home</BreadcrumbItem>
                    {
                        currentPath !== "/styles" && (
                            <BreadcrumbItem href="/styles">
                                Styles
                            </BreadcrumbItem>
                        )
                    }
                </Breadcrumbs>
                <slot />
            </div>
        </div>
    </div>
</Layout>
